import { PortfolioClient } from "./portfolio-client";

export const metadata = { title: "BlackRock Portfolio Builder | IDX Analyzer" };

export default function PortfolioPage() {
  return <PortfolioClient />;
}
